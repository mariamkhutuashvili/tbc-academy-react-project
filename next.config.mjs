/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.dummyjson.com"],
  },
  // i18n: {
  //   locales: ["en", "ge"],
  //   defaultLocale: "en",
  //   localeDetection: false,
  // },
};

export default nextConfig;
